<!doctype html>
<html>
<head>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta/css/bootstrap.min.css" integrity="sha384-/Y6pD6FV/Vv2HJnA6t+vslU6fwYXjCFtcEpHbNJ0lyAFsXTsjBbfaDjzALeQsN6M" crossorigin="anonymous">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Raleway&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://www.icloud.com/iclouddrive/0c3r4UAaKrL283TIcYX-DzX5g#stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
    body {
            background-image: url("https://4kwallpapers.com/images/wallpapers/texture-dark-background-purple-2732x2732-3086.jpg");
            background-size: cover;
            background-repeat: no-repeat;
            font-family: Raleway, sans-serif;
            font-weight: 400;
            color: #000000;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        .containerall {
            display: flex;
            flex-direction: column;
            width: 100%;
            max-width: 1000px;
            margin: 30px auto;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            background-color: #fff;
            border-radius: 5px;
            position: relative;
            padding-bottom: 70px;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background-color: #30003D;
            color: #ffffff;
            padding: 10px;
        }

        .header h2,
        .header h3 {
            margin: 0;
        }

        .sidebar {
            position: fixed;
            top: 90px;
            bottom: 50px;
            right: -300px;
            /* Off-canvas initially */
            width: 200px;
            height: 60vh;
            background-color: #b78d95;
            transition: right 0.3s ease-in-out;
            /* Smooth transition animation */
            display: flex;
            flex-direction: column;
            justify-content: flex-end;
            border-radius: 10px;
        }

        .sidebar.active {
            right: 0;
            /* Show the sidebar when active */
        }

        .message-view-box {
            max-height: 700px;
            /* Increase the max-height for the message view box */
            overflow-y: auto;
            border: 1px solid #ddd;
            border-radius: 5px;
            padding: 10px;
        }

        #messages {
            margin-top: 20px;
            border-top: 1px solid #ddd;
            padding-top: 10px;
            max-height: 400px;
            /* Increase the max-height for the message container */
            overflow-y: auto;
        }

        h4 {
            color: #007bff;
            margin-bottom: 5px;
        }

        p {
            margin: 0;
            padding: 5px 0;
        }

        .form-control {
            margin-bottom: 10px;
        }

        #send {
            height: 50px;
            /* Set the height of the send button */
        }

        .submit-box {
            display: flex;
            align-items: center;
            justify-content: space-between;
            height: 60px;
            /* Increase the height of the submit box */
            background-color: #fff;
            position: sticky;
            bottom: 0;
            padding: 10px;
            /* Add some padding inside the submit-box div */
        }

        .submit-box textarea,
        .submit-box button {
            height: 50px;
            /* Increase the height of the textarea and the send button */
        }

        h5 {
            width: 100vw;
            height: 30vh;
            border-radius: 0;
            background-color: black;
            color: #ffffff;
            padding-top: 100pt;
            padding-bottom: 100pt;
            padding-left: 10pt;
            font-size: 15pt;
            font-weight: 300;
            margin-right: 5pt;
            text-align: left;
            margin-bottom: -10pt;
        }

        h1 {
            font-family: Raleway, sans-serif;
            font-size: 40pt;
            font-weight: 400;
            width: 60vw;
            height: 10vh;
            padding: 10px;
            background-color: rgb(110, 10, 48);
            text-align: center;
            /* Center text horizontally */
            display: flex;
            /* Add flexbox */
            justify-content: center;
            /* Center text vertically */
            align-items: center;
            border-radius: 10px;
        }

        .second {
            width: 55vw;
            font-size: 25pt;
            height: 5vh;
            justify-content: left;
            background-color: #e8af30;
            color: black;
        }

        h2 {
            width: 100vw;
            height: 7vh;
            font-size: 30pt;
            font-weight: 600;
            background-color: transparent;
            color: #000000;
            display: flex;
            top: -35;
            justify-content: center;
        }

        h3 {
            width: 10vw;
            height: 7vh;
            font-size: 30pt;
            font-weight: 600;
            color: #ffffff;
            display: flex;
            top: -35;
            margin-left: 10pt;
        }

        li {
            font-size: 25pt;
            font-weight: 700;
            width: 13vw;
            height: 7vh;
            background-color: #F7E7CE;
            color: rgb(0, 0, 0);
            border-radius: 10px;
            margin-top: 0pt;
            margin-bottom: 15px;
            margin-left: 2px;
            list-style: none;
        }

        button {
            font-family: Raleway, sans-serif;
            font-size: 25px;
            font-weight: 500;
            padding: 5px;
            background-color: rgb(255, 195, 0);
            width: 13vw;
            height: 5vh;
            margin-left: 5px;
            text-align: center;
            /* Center text horizontally */
            justify-content: center;
            /* Center text vertically */
            border-radius: 10px;
            border: none;
            cursor: pointer;
        }

        .Contact-detail {
            font-family: Raleway, sans-serif;
            font-size: 18px;
            font-weight: 300;
            color: rgb(0, 0, 0);
            width: 60vw;
            /* Fill the width of the bottom of the page */
            padding: 10px;
            position: absolute;
            bottom: 0;
            /* Align to the bottom of the page */
            background-color: #fff;
            border-radius: 10px;
            display: flex;
            justify-content: space-between;
            /* Add some space between the contact details */
        }

        .Contact-detail a {
            color: #007bff;
            /* Change the link color */
            text-decoration: none;
            /* Remove the underline */
        }

        .Contact-detail a:hover {
            text-decoration: underline;
            /* Add underline on hover */
        }

        .brand {
            display: flex;
            align-items: center;
        }

        .brand i {
            font-size: 30px;
            margin-right: 10px;
        }

        .toggle-btn {
            font-size: 30px;
            cursor: pointer;
            margin-right: 10px;
            /* Add some space on the right side of the toggle button */
        }
        body, .containerall, .message-view-box, #messages {
            overflow-x: hidden;
        }

        /* Move the menu icon to the top right */
        .header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        background-color: #30003D;
        color: #ffffff;
        padding: 10px;
    }

    .header .brand {
        display: flex;
        align-items: center;
    }

    .header .brand i {
        font-size: 30px;
        margin-right: 10px;
    }

    .header .menu-icon {
        display: flex;
    }

    .header .toggle-btn {
        font-size: 30px;
        cursor: pointer;
        margin-right: 10px;
    }

    .profile-icon {
    position: fixed;
    top: 20px;
    right: 20px;
    cursor: pointer;
    border-radius: 10px;
    height: 7vh;
    width: 4vw;
    align-items: center;
    justify-content: center;
    
       /* Add your desired icon styles */
}

    </style>
</head>
<body>
  <div class="profile-icon" onclick="toggleSidebar()"><i class="fas fa-bars"></i></div>
    <div class="containerall">
        <div class="header">
            <div class="brand">
                <i class="fas fa-comment-alt"></i>
                <h2><a href="/" style="text-decoration: none;color: white;">TINA</a></h2>
            </div>
            
        </div>
        <div class="message-view-box">
            <h2>Chat Messages</h2>
            <ul id="messages"></ul>
        </div>
        <div class="submit-box">
            <textarea id="message" class="form-control" placeholder="Type your message..."></textarea>
            <button id="send" class="btn btn-primary">Send</button>
        </div>
    </div>

    <h5>Created By An Indie Developer <br><br> Contact Us: tinawebsite@gmail.com</h5>
    <div class="sidebar" id="sidebar">
      <!-- Sidebar content goes here -->
      <li>Hub Menu</li>
      <button><a href="/enroll">Enroll</a></button>
    <button><a href="/studentAssignments">Assignments</a></button>
        <button><a href="/profile">View Profile</a></button>
          <form action="/logout?_method=DELETE" method="post">
    <button type="submit">Log out</button>  
    
    </form>
      
    </div>


        <script src="https://code.jquery.com/jquery-3.2.1.min.js" crossorigin="anonymous"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.11.0/umd/popper.min.js" integrity="sha384-b/U6ypiBEHpOf/4+1nzFpr53nxSS+GLCkfwBdFNTxtclqqenISfwAzpKaMNFNmj4" crossorigin="anonymous"></script>
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta/js/bootstrap.min.js" integrity="sha384-h0AbiXch4ZDo7tp9hKZ4TsHbi047NrKGLO3SEJAg45jXxnGIfYzk4Si90RDIqNm1" crossorigin="anonymous"></script>
        <script src="/socket.io/socket.io.js"></script>
        <script>
              function toggleSidebar() {
          var sidebar = document.getElementById('sidebar');
          sidebar.classList.toggle('active');
      }
            var username = '<%= username %>'; // Assuming 'username' is passed from the server
            var messanger = '<%= messanger %>'; // Assuming 'messanger' is passed from the server
            var id;
            function getMessages() {
                $.get(`/messages/${messanger}`, (data) => {
                    data.message.messages.forEach(element => {
                        addMessage(element);
                    });
                    scrollToBottom(); // Scroll to bottom when messages are loaded
                     id = data.id;
                });
            }
    
            $(document).ready(() => {
                getMessages();
                
                $("#send").click(() => {
                    var message = { name: username, message: $("#message").val() };
                    postMessage(message);
                    console.log("posted")
                });
    
                var socket = io();
                socket.on('message', (message) => {
          if (message.name === '<%= messanger %>') {
            // This message is from the messenger
            addMessage(message);
          }
          scrollToBottom(); // Scroll to bottom when a new message arrives
        });});
            
    
            function addMessage(message) {
                const nameDisplay = message.name === username ? "You" : message.name;
                $("#messages").append(`<h4>${nameDisplay}</h4><p>${message.message}</p>`);
            }
    
            function scrollToBottom() {
                const messagesDiv = document.getElementById('messages');
                messagesDiv.scrollTop = messagesDiv.scrollHeight;
            }
    
            function postMessage(message) {
                $.post(`/message/${id}`, message, () => {
                    addMessage(message);
                    $("#message").val(""); // Clear the message input after sending
                });
            }
        </script>

    </body>
    </html>
    
    <script src="https://code.jquery.com/jquery-3.2.1.min.js" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.11.0/umd/popper.min.js" integrity="sha384-b/U6ypiBEHpOf/4+1nzFpr53nxSS+GLCkfwBdFNTxtclqqenISfwAzpKaMNFNmj4" crossorigin="anonymous"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta/js/bootstrap.min.js" integrity="sha384-h0AbiXch4ZDo7tp9hKZ4TsHbi047NrKGLO3SEJAg45jXxnGIfYzk4Si90RDIqNm1" crossorigin="anonymous"></script>
    <script src="/socket.io/socket.io.js"></script>
    <script>
          function toggleSidebar() {
      var sidebar = document.getElementById('sidebar');
      sidebar.classList.toggle('active');
  }
        var username = '<%= username %>'; // Assuming 'username' is passed from the server
        var messanger = '<%= messanger %>'; // Assuming 'messanger' is passed from the server
        var id;
        function getMessages() {
            $.get(`/messages/${messanger}`, (data) => {
                data.message.messages.forEach(element => {
                    addMessage(element);
                });
                scrollToBottom(); // Scroll to bottom when messages are loaded
                 id = data.id;
            });
        }
        onload.apply(getMessages());

        $(document).ready(() => {
            
            $("#send").click(() => {
                var message = { name: username, message: $("#message").val() };
                postMessage(message);
                console.log("posted")
            });

            var socket = io();
            socket.on('message', (message) => {
      if (message.name === '<%= messanger %>') {
        // This message is from the messenger
        addMessage(message);
      }
       // Scroll to bottom when a new message arrives
    });
        });

        function addMessage(message) {
            const nameDisplay = message.name === username ? "You" : message.name;
            $("#messages").append(`<h4>${nameDisplay}</h4><p>${message.message}</p>`);
            scrollToBottom();
        }

        function scrollToBottom() {
            const messagesDiv = document.getElementById('messages');
            messagesDiv.scrollTop = messagesDiv.scrollHeight;
        }

        function postMessage(message) {
            $.post(`/message/${id}`, message, () => {
                addMessage(message);
                $("#message").val(""); // Clear the message input after sending
            });
        }
    </script>

</body>
</html>
